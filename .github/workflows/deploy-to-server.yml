# this will be deploy the safe/ secure/tested app to prod server
name: Deploy to Server

on:
  #  push:
  #    branches: [main]
  workflow_call:

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: SSH to Prod server
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.EC2_SERVER_HOST }}
          username: ${{ secrets.EC2_SERVER_USER }}
          key: ${{ secrets.EC2_SERVER_KEY }}
          script: |
            echo "Staring Deployment"
            mkdir devops
